name: flutter_spotlight_tour_example
description: Example app for flutter_spotlight_tour.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_spotlight_tour:
    path: ../

flutter:
  uses-material-design: true
